'use client';
import {Children,isValidElement,useState,type ReactNode} from 'react';
export default function TeacherPages({children}:{children:ReactNode}){const [page,setPage]=useState(0);const sections=Children.toArray(children).flatMap(child=>isValidElement<{className?:string;children?:ReactNode}>(child)&&child.props.className==='teacher-grid'?Children.toArray(child.props.children):[child]).filter(child=>isValidElement(child)&&child.type!=='p');return <div className="teacher-pages"><div className="teacher-page">{sections[page]}</div><nav className="view-switch"><button disabled={page===0} onClick={()=>setPage(page-1)}>← Previous</button><span>{page+1} / {sections.length}</span><button disabled={page===sections.length-1} onClick={()=>setPage(page+1)}>Next →</button></nav></div>}
